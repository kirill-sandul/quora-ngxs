import { SearchTagsPipe } from './search-tags.pipe';

describe('SearchTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTagsPipe();
    expect(pipe).toBeTruthy();
  });
});
