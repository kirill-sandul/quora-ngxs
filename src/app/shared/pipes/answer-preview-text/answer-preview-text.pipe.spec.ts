import { AnswerPreviewTextPipe } from './answer-preview-text.pipe';

describe('AnswerPreviewTextPipe', () => {
  it('create an instance', () => {
    const pipe = new AnswerPreviewTextPipe();
    expect(pipe).toBeTruthy();
  });
});
