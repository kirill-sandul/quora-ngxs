import { AnswersLengthPipe } from './answers-length.pipe';

describe('AnswersLengthPipe', () => {
  it('create an instance', () => {
    const pipe = new AnswersLengthPipe();
    expect(pipe).toBeTruthy();
  });
});
